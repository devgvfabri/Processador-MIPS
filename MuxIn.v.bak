module MuxIn(imediato, entrada, in, saida);
input [13:0] imediato, entrada;
input in;
output [31:0] saida;

always@(*)
	begin
		if(in)
		begin
			saida = entrada;
		end
		else
		begin
			saida = imediato;
		end
	end
	
endmodule